package library.lib.commands;

import edu.wpi.first.wpilibj.command.CommandGroup;

/**
 *does nothing. not sure how this works without any return in the method but
 *it came with the thinggy so I wont question CTRE
 */
public class DoNothing extends CommandGroup {

	public DoNothing() {
	}
}
